const apartmentsPath = {
    getAll: 'apartment'
}

const edifficePath = {
    getAll: 'ediffice'
}

const seasonPath = {
    getAll: '/season'
}

module.exports = {
    apartmentsPath,
    edifficePath,
    seasonPath
}